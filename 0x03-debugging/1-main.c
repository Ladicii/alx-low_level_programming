#include <stdio.h>

/**
 * main - causes an infinite loop
 * Return:0
 */

int main(void)
{
	int i;

	printf("Infinite loop incoming :(\n");

	i = 0;
	/**
	 * while(i < 10)
	 * {
	 * putchar
	 * }
	 */
	printf("Infinte loop avaoided! \\0/\n"0;

	return (0);
}
